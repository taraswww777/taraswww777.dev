import { MetaHead } from 'src/components/mdx/MetaHead';
import { PageTemplate } from 'src/components/PageTemplate';
import { VerticalNav } from 'src/components/vertical-nav';
import { LINKS } from 'src/constants/links';
import "src/globals.css";
import { linksToVerticalNavItems } from 'src/utils/linksToVerticalNavItems';
import { ContentContainer } from '../../ui/ContentContainer';
import MdxLayout from '../../app/MdxLayout';


<MetaHead
  title={LINKS.articlesIndex.title}
  ogCanonicalUrl={LINKS.articlesIndex.link}
/>
<MdxLayout>
  <PageTemplate>
    <ContentContainer>
      <div className="w-full">
        <VerticalNav items={linksToVerticalNavItems(LINKS.articles)} />
      </div>
    </ContentContainer>
  </PageTemplate>
</MdxLayout>
